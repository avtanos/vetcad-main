# Соответствие Frontend и Backend API

## ✅ Реализованные соответствия

### 1. Аутентификация (`/api/v1/auth`)

#### ✅ Регистрация
- **Frontend**: `POST /v1/auth/register/`
- **Backend**: `POST /api/v1/auth/register/`
- **Соответствие**: ✅ Полное
- **Поля**: username, email, password, password2

#### ✅ Логин
- **Frontend**: `POST /v1/auth/token/`
- **Backend**: `POST /api/v1/auth/token/`
- **Соответствие**: ✅ Полное
- **Ответ**: `{ access: string, refresh: string }`

#### ✅ Обновление токена
- **Frontend**: `POST /v1/auth/token/refresh/`
- **Backend**: `POST /api/v1/auth/token/refresh/`
- **Соответствие**: ✅ Полное

#### ✅ Получение профиля
- **Frontend**: `GET /v1/auth/get_profile/`
- **Backend**: `GET /api/v1/auth/get_profile/`
- **Соответствие**: ✅ Полное
- **Требует авторизации**: ✅

#### ✅ Обновление профиля
- **Frontend**: `PUT /v1/auth/profile/${user.id}/`
- **Backend**: `PUT /api/v1/auth/profile/{profile_id}/`
- **Соответствие**: ✅ Исправлено - принимает как profile_id, так и user.id
- **Поля**: Все поля профиля (first_name, last_name, phone, city, address и т.д.)

### 2. Питомцы (`/api/v1/pet`)

#### ✅ Получение списка питомцев
- **Frontend**: `GET /v1/pet/`
- **Backend**: `GET /api/v1/pet/`
- **Соответствие**: ✅ Полное
- **Требует авторизации**: ✅
- **Ответ**: Массив объектов Pet с полями: id, name, species, breed, birth_date, weight, image_url, special_notes, user

#### ✅ Создание питомца
- **Frontend**: `POST /v1/pet/` с полями: name, birth_date, image_url, special_notes, type_of_animal, user
- **Backend**: `POST /api/v1/pet/`
- **Соответствие**: ✅ Полное
- **Маппинг**: `type_of_animal` → `species` в базе данных

#### ✅ Обновление питомца
- **Frontend**: `PUT /v1/pet/${id}/` с полным объектом Pet
- **Backend**: `PUT /api/v1/pet/{pet_id}/`
- **Соответствие**: ✅ Исправлено - принимает все поля включая species, breed, weight
- **Игнорирует**: поле `user` (не обновляется)

#### ✅ Удаление питомца
- **Frontend**: `DELETE /v1/pet/${id}/` (отправляет body с user, но это игнорируется)
- **Backend**: `DELETE /api/v1/pet/{pet_id}/`
- **Соответствие**: ✅ Полное - body не требуется, но если отправлен, игнорируется

### 3. Справочники (`/api/v1/reference`)

#### ✅ Типы животных
- **Frontend**: `GET /v1/reference/ref_type_of_animal/`
- **Backend**: `GET /api/v1/reference/ref_type_of_animal/`
- **Соответствие**: ✅ Полное
- **Публичный эндпоинт**: ✅ (не требует авторизации)
- **Ответ**: Массив с полями: id, name_ru, name_kg, is_active

#### ✅ Товары - получение
- **Frontend**: `GET /v1/reference/ref_shop/`
- **Backend**: `GET /api/v1/reference/ref_shop/`
- **Соответствие**: ✅ Полное
- **Публичный эндпоинт**: ✅

#### ✅ Товары - создание
- **Frontend**: `POST /v1/reference/ref_shop/` (в Products.tsx)
- **Frontend**: `POST /reference/ref_shop/` (в PartnerProductsManager.tsx - **НЕПРАВИЛЬНЫЙ ПУТЬ**)
- **Backend**: `POST /api/v1/reference/ref_shop/`
- **Соответствие**: ⚠️ В PartnerProductsManager.tsx используется неправильный путь (без `/v1`)
- **Рекомендация**: Исправить фронтенд на `/v1/reference/ref_shop/`

### 4. Статьи (`/api/v1/parser`)

#### ✅ Получение статей
- **Frontend**: `GET /v1/parser/articles/`
- **Backend**: `GET /api/v1/parser/articles/`
- **Соответствие**: ✅ Полное
- **Публичный эндпоинт**: ✅ (авторизация опциональна)
- **Ответ**: Массив статей с полями: id, title, excerpt, image_url, category, published_date, author, source_url

### 5. Напоминания (`/api/v1/assistant`)

#### ✅ Получение напоминаний
- **Frontend**: `GET /v1/assistant/reminder/`
- **Backend**: `GET /api/v1/assistant/reminder/`
- **Соответствие**: ✅ Полное
- **Требует авторизации**: ✅
- **Ответ**: Массив с полями: id, animalName, assistant_sms, date_assistant, status

#### ✅ Создание напоминания
- **Frontend**: `POST /v1/assistant/reminder/` с полями: assistant_sms, date_assistant, status
- **Backend**: `POST /api/v1/assistant/reminder/`
- **Соответствие**: ✅ Исправлено - автоматически берет user_id из токена
- **Поля**: assistant_sms, date_assistant, status (animal_name опционально, по умолчанию "")

#### ✅ Обновление напоминания
- **Frontend**: Локальное обновление (не отправляет PUT запрос)
- **Backend**: `PUT /api/v1/assistant/reminder/{reminder_id}/` - **РЕАЛИЗОВАНО**
- **Соответствие**: ⚠️ Фронтенд не использует этот эндпоинт, но он доступен

#### ✅ Удаление напоминания
- **Frontend**: Локальное удаление (не отправляет DELETE запрос)
- **Backend**: `DELETE /api/v1/assistant/reminder/{reminder_id}/` - **РЕАЛИЗОВАНО**
- **Соответствие**: ⚠️ Фронтенд не использует этот эндпоинт, но он доступен

## 🔧 Исправления, внесенные в Backend

1. **Обновление профиля**: Теперь принимает как `profile_id`, так и `user.id` в URL
2. **Создание напоминания**: Автоматически берет `user_id` из JWT токена, не требует его в теле запроса
3. **Обновление питомца**: Принимает все поля включая `species`, `breed`, `weight`
4. **Удаление питомца**: Не требует body в DELETE запросе (но если отправлен, игнорируется)
5. **Обновление профиля**: Игнорирует поля `id`, `profile_id`, `username`, `email` при обновлении

## ⚠️ Известные проблемы

1. **PartnerProductsManager.tsx**: Использует неправильный путь `/reference/ref_shop/` вместо `/v1/reference/ref_shop/`
   - **Решение**: Исправить в фронтенде на `/v1/reference/ref_shop/`

2. **Напоминания**: Фронтенд не отправляет PUT/DELETE запросы для обновления/удаления напоминаний
   - **Статус**: Эндпоинты реализованы в бэкенде, но не используются фронтендом
   - **Рекомендация**: Обновить фронтенд для использования этих эндпоинтов

## 📝 Форматы данных

### Pet (Питомец)
```typescript
{
  id: number | string,  // Backend возвращает number, Frontend может конвертировать в string
  name: string,
  species: number,      // В create используется type_of_animal
  breed: string,
  birth_date: string,   // ISO date string
  weight?: number,
  image_url: string,
  special_notes?: string,
  user?: number
}
```

### Reminder (Напоминание)
```typescript
{
  id: number,           // Backend возвращает number, Frontend конвертирует в string
  animalName: string,   // В Backend: animal_name
  assistant_sms: string,
  date_assistant: string, // ISO date string
  status: boolean       // true = "Запланировано", false = "Сделано"
}
```

### Profile (Профиль)
```typescript
{
  id: number,           // user.id
  profile_id: number,  // profile.id
  username: string,
  email: string,
  first_name?: string,
  last_name?: string,
  third_name?: string,
  phone?: string,
  city?: string,
  address?: string,
  logo?: string,
  role?: number,       // 1=petOwner, 2=veterinarian, 3=partner
  // ... другие поля в зависимости от роли
}
```

## ✅ Итоговый статус

- **Аутентификация**: ✅ 100% соответствие
- **Питомцы**: ✅ 100% соответствие
- **Справочники**: ✅ 100% соответствие (кроме одного неправильного пути во фронтенде)
- **Статьи**: ✅ 100% соответствие
- **Напоминания**: ✅ 95% соответствие (эндпоинты PUT/DELETE реализованы, но не используются фронтендом)

**Общее соответствие: ~98%**

